/*archivo.js que se utilizaria para traer los datos del from para poder actualizarlos
  pero no pude obtener el id de la publicacion a si que quedo sin usar*/

/*import { actualizarPublicacion } from './crud-post.js';

const btn_actualiza = document.getElementById('tbn_guardar');

btn_actualiza.addEventListener('submit', async(e)=>{
    e.preventDefault();

    const id_actual = document.getElementById('idPublicacion'); 

    const respuesta = await actualizarPublicacion(id_actual);

    alert(respuesta.msg);
    location.href='/'
   
})*/